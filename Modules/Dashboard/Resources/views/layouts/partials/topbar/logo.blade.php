<div class="navbar-brand-box">
    <a href="{{ route('home') }}" class="logo logo-light">
                        <span class="logo-sm">
                            <img src="{{ app_logo() }}" alt="{{ site_name() }}" height="60" width="150">
                        </span>
        <span class="logo-lg">
                            <img src="{{ app_logo() }}" alt="{{ site_name() }}" height="60" width="150">
                        </span>
    </a>
</div>
